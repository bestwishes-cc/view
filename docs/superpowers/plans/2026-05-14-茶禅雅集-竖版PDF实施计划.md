# 茶禅雅集 · 老友节高客服务发布会 竖版PDF实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 生成6页竖版PDF，展示茶禅雅集活动的高端氛围，供客户手机阅读

**Architecture:** 使用Python reportlab库生成PDF，采用现代奢华风设计（深蓝+金色+米白），每页包含大图背景和简洁文字内容

**Tech Stack:** Python, reportlab, Pillow (图片处理)

---

## 文件结构

```
/Users/cc/Documents/库/思考/活动策划/0427 5月隐湖云舍高客活动策划/
├── generate_pdf.py              # 主生成脚本
├── assets/
│   ├── images/                  # 图片素材目录
│   │   ├── cover.jpg           # 封面大图
│   │   ├── tea.jpg             # 茶韵体验图
│   │   ├── zen.jpg             # 禅意时光图
│   │   ├── community.jpg       # 社区环境图
│   │   └── abstract.jpg        # 抽象意境图
│   └── fonts/                   # 字体目录
│       └── SourceHanSansSC.ttf  # 思源黑体
├── output/
│   └── 茶禅雅集-老友节高客服务发布会.pdf  # 输出PDF
└── docs/
    └── superpowers/
        ├── specs/               # 设计规格
        └── plans/               # 实施计划
```

## 实施任务

### Task 1: 环境准备和依赖安装

**Files:**
- Create: `requirements.txt`

- [ ] **Step 1: 创建requirements.txt**

```
reportlab==4.0.4
Pillow==10.0.1
```

- [ ] **Step 2: 安装依赖**

Run: `pip install -r requirements.txt`
Expected: Successfully installed reportlab and Pillow

- [ ] **Step 3: 验证安装**

Run: `python3 -c "import reportlab; print('reportlab installed')"`
Expected: "reportlab installed"

- [ ] **Step 4: Commit**

```bash
git add requirements.txt
git commit -m "chore: add PDF generation dependencies"
```

---

### Task 2: 准备图片素材

**Files:**
- Create: `download_images.py`

- [ ] **Step 1: 创建图片下载脚本**

```python
#!/usr/bin/env python3
"""下载图片素材"""
import os
import requests
from pathlib import Path

# 创建目录
Path("assets/images").mkdir(parents=True, exist_ok=True)

# 图片URL（示例，需要替换为真实图片）
images = {
    "cover.jpg": "https://example.com/tea-zen-cover.jpg",
    "tea.jpg": "https://example.com/tea-garden.jpg",
    "zen.jpg": "https://example.com/lingyin-temple.jpg",
    "community.jpg": "https://example.com/yinhu-community.jpg",
    "abstract.jpg": "https://example.com/abstract-art.jpg"
}

for filename, url in images.items():
    filepath = f"assets/images/{filename}"
    if not os.path.exists(filepath):
        print(f"Downloading {filename}...")
        # 注意：实际使用时需要替换为真实图片URL
        # response = requests.get(url)
        # with open(filepath, 'wb') as f:
        #     f.write(response.content)
        print(f"  -> 请手动下载图片到 {filepath}")
    else:
        print(f"{filename} already exists")
```

- [ ] **Step 2: 从原PDF提取图片**

Run: `pdfimages -j "杭州隐湖云舍旅居社区二季度险端赋能方案.pdf" assets/images/pdf_extract`
Expected: 提取PDF中的图片到assets/images目录

- [ ] **Step 3: 整理图片素材**

将提取的图片重命名并移动到对应位置：
- 选择茶园相关图片作为 `tea.jpg`
- 选择灵隐寺相关图片作为 `zen.jpg`
- 选择社区环境图片作为 `community.jpg`

- [ ] **Step 4: Commit**

```bash
git add assets/images/
git commit -m "feat: add image assets for PDF generation"
```

---

### Task 3: 准备中文字体

**Files:**
- Create: `download_font.py`

- [ ] **Step 1: 创建字体下载脚本**

```python
#!/usr/bin/env python3
"""下载中文字体"""
import os
from pathlib import Path

# 创建目录
Path("assets/fonts").mkdir(parents=True, exist_ok=True)

font_path = "assets/fonts/SourceHanSansSC-Regular.otf"

if not os.path.exists(font_path):
    print("下载思源黑体字体...")
    # 从Google Fonts下载
    url = "https://github.com/adobe-fonts/source-han-sans/raw/release/OTF/SimplifiedChinese/SourceHanSansSC-Regular.otf"
    print(f"  -> 请手动下载字体到 {font_path}")
    print(f"  -> 下载地址: {url}")
else:
    print("字体文件已存在")
```

- [ ] **Step 2: 下载字体**

从以下地址下载思源黑体：
https://github.com/adobe-fonts/source-han-sans/raw/release/OTF/SimplifiedChinese/SourceHanSansSC-Regular.otf

保存到 `assets/fonts/SourceHanSansSC-Regular.otf`

- [ ] **Step 3: 验证字体**

Run: `python3 -c "from reportlab.pdfbase.ttfonts import TTFont; TTFont('SourceHanSans', 'assets/fonts/SourceHanSansSC-Regular.otf'); print('Font loaded successfully')"`
Expected: "Font loaded successfully"

- [ ] **Step 4: Commit**

```bash
git add assets/fonts/
git commit -m "feat: add Chinese font for PDF generation"
```

---

### Task 4: 创建PDF生成主脚本

**Files:**
- Create: `generate_pdf.py`

- [ ] **Step 1: 创建基础脚本结构**

```python
#!/usr/bin/env python3
"""生成茶禅雅集活动PDF"""
from reportlab.lib.pagesizes import portrait
from reportlab.lib.units import inch
from reportlab.pdfgen import canvas
from reportlab.lib.colors import HexColor
from reportlab.pdfbase import pdfmetrics
from reportlab.pdfbase.ttfonts import TTFont
from PIL import Image
import os

# 页面尺寸（9:16竖版）
PAGE_WIDTH = 6 * inch  # 1080px at 180dpi
PAGE_HEIGHT = 10.67 * inch  # 1920px at 180dpi

# 颜色定义
DARK_BLUE = HexColor('#1a365d')
GOLD = HexColor('#d4af37')
LIGHT_GRAY = HexColor('#f8f9fa')
WHITE = HexColor('#ffffff')

# 注册字体
pdfmetrics.registerFont(TTFont('SourceHanSans', 'assets/fonts/SourceHanSansSC-Regular.otf'))

def create_pdf():
    """创建PDF文档"""
    output_path = "output/茶禅雅集-老友节高客服务发布会.pdf"
    os.makedirs("output", exist_ok=True)
    
    c = canvas.Canvas(output_path, pagesize=(PAGE_WIDTH, PAGE_HEIGHT))
    
    # 生成各页面
    create_cover_page(c)
    create_overview_page(c)
    create_tea_page(c)
    create_zen_page(c)
    create_community_page(c)
    create_contact_page(c)
    
    c.save()
    print(f"PDF生成完成: {output_path}")

def create_cover_page(c):
    """创建封面页"""
    # TODO: 实现封面页
    c.showPage()

def create_overview_page(c):
    """创建活动概要页"""
    # TODO: 实现活动概要页
    c.showPage()

def create_tea_page(c):
    """创建茶韵体验页"""
    # TODO: 实现茶韵体验页
    c.showPage()

def create_zen_page(c):
    """创建禅意时光页"""
    # TODO: 实现禅意时光页
    c.showPage()

def create_community_page(c):
    """创建品牌介绍页"""
    # TODO: 实现品牌介绍页
    c.showPage()

def create_contact_page(c):
    """创建报名咨询页"""
    # TODO: 实现报名咨询页
    c.showPage()

if __name__ == "__main__":
    create_pdf()
```

- [ ] **Step 2: 测试基础脚本**

Run: `python3 generate_pdf.py`
Expected: 生成空白PDF文件

- [ ] **Step 3: Commit**

```bash
git add generate_pdf.py
git commit -m "feat: create PDF generation script structure"
```

---

### Task 5: 实现封面页

**Files:**
- Modify: `generate_pdf.py`

- [ ] **Step 1: 实现封面页函数**

```python
def create_cover_page(c):
    """创建封面页"""
    # 绘制背景图片
    try:
        img = Image.open("assets/images/cover.jpg")
        img_width, img_height = img.size
        ratio = min(PAGE_WIDTH / img_width, PAGE_HEIGHT / img_height)
        new_width = img_width * ratio
        new_height = img_height * ratio
        x = (PAGE_WIDTH - new_width) / 2
        y = (PAGE_HEIGHT - new_height) / 2
        c.drawImage("assets/images/cover.jpg", x, y, new_width, new_height)
    except Exception as e:
        print(f"封面图片加载失败: {e}")
        # 使用纯色背景
        c.setFillColor(DARK_BLUE)
        c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    
    # 绘制渐变遮罩
    c.setFillColor(DARK_BLUE)
    c.setFillAlpha(0.7)
    c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    c.setFillAlpha(1)
    
    # 顶部徽章
    badge_width = 1.5 * inch
    badge_height = 0.4 * inch
    badge_x = (PAGE_WIDTH - badge_width) / 2
    badge_y = PAGE_HEIGHT - 1 * inch
    
    c.setFillColor(GOLD)
    c.roundRect(badge_x, badge_y, badge_width, badge_height, 10, fill=1)
    
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 12)
    text_x = PAGE_WIDTH / 2
    text_y = badge_y + badge_height / 2 - 4
    c.drawCentredString(text_x, text_y, "尊享邀约")
    
    # 品牌标识
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 14)
    c.drawCentredString(PAGE_WIDTH / 2, PAGE_HEIGHT - 2.5 * inch, "大家的家 · 隐湖云舍")
    
    # 主标题
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 36)
    c.drawCentredString(PAGE_WIDTH / 2, PAGE_HEIGHT - 4 * inch, "茶禅雅集")
    
    # 副标题
    c.setFont('SourceHanSans', 18)
    c.drawCentredString(PAGE_WIDTH / 2, PAGE_HEIGHT - 4.8 * inch, "老友节高客服务发布会")
    
    # 底部信息
    c.setFillColor(WHITE)
    c.setFillAlpha(0.8)
    c.setFont('SourceHanSans', 12)
    c.drawCentredString(PAGE_WIDTH / 2, 1.5 * inch, "2026年5月29-30日")
    c.drawCentredString(PAGE_WIDTH / 2, 1.2 * inch, "杭州 · 西湖畔")
    c.setFillAlpha(1)
    
    c.showPage()
```

- [ ] **Step 2: 测试封面页**

Run: `python3 generate_pdf.py`
Expected: 生成包含封面页的PDF

- [ ] **Step 3: Commit**

```bash
git add generate_pdf.py
git commit -m "feat: implement cover page with image background"
```

---

### Task 6: 实现活动概要页

**Files:**
- Modify: `generate_pdf.py`

- [ ] **Step 1: 实现活动概要页函数**

```python
def create_overview_page(c):
    """创建活动概要页"""
    # 绘制背景图片
    try:
        c.drawImage("assets/images/abstract.jpg", 0, 0, PAGE_WIDTH, PAGE_HEIGHT)
    except Exception as e:
        print(f"概要页图片加载失败: {e}")
        c.setFillColor(LIGHT_GRAY)
        c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    
    # 绘制遮罩
    c.setFillColor(DARK_BLUE)
    c.setFillAlpha(0.8)
    c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    c.setFillAlpha(1)
    
    # 标题区域
    title_bg_height = 1.5 * inch
    title_bg_y = PAGE_HEIGHT - 2 * inch
    
    c.setFillColor(DARK_BLUE)
    c.rect(0, title_bg_y, PAGE_WIDTH, title_bg_height, fill=1)
    
    # 小标题
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 12)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + title_bg_height - 0.5 * inch, "茶韵禅意")
    
    # 主标题
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 24)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + 0.3 * inch, "两天一夜沉浸式体验")
    
    # 描述内容
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 14)
    
    descriptions = [
        "在西湖畔的园林中",
        "体验茶道与禅修的雅致时光",
        "",
        "尊享专属",
        "高端定制",
        "文化沉浸"
    ]
    
    y_position = PAGE_HEIGHT - 4 * inch
    for line in descriptions:
        if line:
            c.drawCentredString(PAGE_WIDTH / 2, y_position, line)
        y_position -= 0.4 * inch
    
    # 底部关键词
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 12)
    keywords = "茶韵 · 禅意 · 尊享 · 沉浸"
    c.drawCentredString(PAGE_WIDTH / 2, 1.5 * inch, keywords)
    
    c.showPage()
```

- [ ] **Step 2: 测试概要页**

Run: `python3 generate_pdf.py`
Expected: 生成包含概要页的PDF

- [ ] **Step 3: Commit**

```bash
git add generate_pdf.py
git commit -m "feat: implement overview page"
```

---

### Task 7: 实现茶韵体验页

**Files:**
- Modify: `generate_pdf.py`

- [ ] **Step 1: 实现茶韵体验页函数**

```python
def create_tea_page(c):
    """创建茶韵体验页"""
    # 绘制背景图片
    try:
        c.drawImage("assets/images/tea.jpg", 0, 0, PAGE_WIDTH, PAGE_HEIGHT)
    except Exception as e:
        print(f"茶韵页图片加载失败: {e}")
        c.setFillColor(LIGHT_GRAY)
        c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    
    # 绘制遮罩
    c.setFillColor(DARK_BLUE)
    c.setFillAlpha(0.6)
    c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    c.setFillAlpha(1)
    
    # 标题区域
    title_bg_height = 1.5 * inch
    title_bg_y = PAGE_HEIGHT - 2 * inch
    
    c.setFillColor(DARK_BLUE)
    c.rect(0, title_bg_y, PAGE_WIDTH, title_bg_height, fill=1)
    
    # 小标题
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 12)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + title_bg_height - 0.5 * inch, "第一日 · 茶")
    
    # 主标题
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 24)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + 0.3 * inch, "尊享茶道体验")
    
    # 描述内容
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 14)
    
    descriptions = [
        "宋韵点茶",
        "田园雅趣",
        "西湖春茶",
        "",
        "在茶园中感受自然",
        "在茶香中品味人生"
    ]
    
    y_position = PAGE_HEIGHT - 4 * inch
    for line in descriptions:
        if line:
            c.drawCentredString(PAGE_WIDTH / 2, y_position, line)
        y_position -= 0.4 * inch
    
    # 底部关键词
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 12)
    keywords = "茶韵 · 雅趣 · 自然 · 品味"
    c.drawCentredString(PAGE_WIDTH / 2, 1.5 * inch, keywords)
    
    c.showPage()
```

- [ ] **Step 2: 测试茶韵体验页**

Run: `python3 generate_pdf.py`
Expected: 生成包含茶韵体验页的PDF

- [ ] **Step 3: Commit**

```bash
git add generate_pdf.py
git commit -m "feat: implement tea experience page"
```

---

### Task 8: 实现禅意时光页

**Files:**
- Modify: `generate_pdf.py`

- [ ] **Step 1: 实现禅意时光页函数**

```python
def create_zen_page(c):
    """创建禅意时光页"""
    # 绘制背景图片
    try:
        c.drawImage("assets/images/zen.jpg", 0, 0, PAGE_WIDTH, PAGE_HEIGHT)
    except Exception as e:
        print(f"禅意页图片加载失败: {e}")
        c.setFillColor(LIGHT_GRAY)
        c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    
    # 绘制遮罩
    c.setFillColor(DARK_BLUE)
    c.setFillAlpha(0.6)
    c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    c.setFillAlpha(1)
    
    # 标题区域
    title_bg_height = 1.5 * inch
    title_bg_y = PAGE_HEIGHT - 2 * inch
    
    c.setFillColor(DARK_BLUE)
    c.rect(0, title_bg_y, PAGE_WIDTH, title_bg_height, fill=1)
    
    # 小标题
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 12)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + title_bg_height - 0.5 * inch, "第二日 · 禅")
    
    # 主标题
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 24)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + 0.3 * inch, "灵隐探秘")
    
    # 描述内容
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 14)
    
    descriptions = [
        "古刹禅境",
        "静谧时光",
        "身心净化",
        "",
        "在灵隐感受禅意",
        "在古刹中净化心灵"
    ]
    
    y_position = PAGE_HEIGHT - 4 * inch
    for line in descriptions:
        if line:
            c.drawCentredString(PAGE_WIDTH / 2, y_position, line)
        y_position -= 0.4 * inch
    
    # 底部关键词
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 12)
    keywords = "禅意 · 古刹 · 净化 · 静谧"
    c.drawCentredString(PAGE_WIDTH / 2, 1.5 * inch, keywords)
    
    c.showPage()
```

- [ ] **Step 2: 测试禅意时光页**

Run: `python3 generate_pdf.py`
Expected: 生成包含禅意时光页的PDF

- [ ] **Step 3: Commit**

```bash
git add generate_pdf.py
git commit -m "feat: implement zen experience page"
```

---

### Task 9: 实现品牌介绍页

**Files:**
- Modify: `generate_pdf.py`

- [ ] **Step 1: 实现品牌介绍页函数**

```python
def create_community_page(c):
    """创建品牌介绍页"""
    # 绘制背景图片
    try:
        c.drawImage("assets/images/community.jpg", 0, 0, PAGE_WIDTH, PAGE_HEIGHT)
    except Exception as e:
        print(f"社区页图片加载失败: {e}")
        c.setFillColor(LIGHT_GRAY)
        c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    
    # 绘制遮罩
    c.setFillColor(DARK_BLUE)
    c.setFillAlpha(0.7)
    c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    c.setFillAlpha(1)
    
    # 标题区域
    title_bg_height = 1.5 * inch
    title_bg_y = PAGE_HEIGHT - 2 * inch
    
    c.setFillColor(DARK_BLUE)
    c.rect(0, title_bg_y, PAGE_WIDTH, title_bg_height, fill=1)
    
    # 小标题
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 12)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + title_bg_height - 0.5 * inch, "高端旅居社区")
    
    # 主标题
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 24)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + 0.3 * inch, "隐湖云舍")
    
    # 描述内容
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 14)
    
    descriptions = [
        "西湖畔",
        "美学宿集",
        "宋韵园林",
        "",
        "大家的家",
        "高端旅居品牌"
    ]
    
    y_position = PAGE_HEIGHT - 4 * inch
    for line in descriptions:
        if line:
            c.drawCentredString(PAGE_WIDTH / 2, y_position, line)
        y_position -= 0.4 * inch
    
    # 底部关键词
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 12)
    keywords = "西湖 · 美学 · 园林 · 高端"
    c.drawCentredString(PAGE_WIDTH / 2, 1.5 * inch, keywords)
    
    c.showPage()
```

- [ ] **Step 2: 测试品牌介绍页**

Run: `python3 generate_pdf.py`
Expected: 生成包含品牌介绍页的PDF

- [ ] **Step 3: Commit**

```bash
git add generate_pdf.py
git commit -m "feat: implement brand introduction page"
```

---

### Task 10: 实现报名咨询页

**Files:**
- Modify: `generate_pdf.py`

- [ ] **Step 1: 实现报名咨询页函数**

```python
def create_contact_page(c):
    """创建报名咨询页"""
    # 简洁背景
    c.setFillColor(LIGHT_GRAY)
    c.rect(0, 0, PAGE_WIDTH, PAGE_HEIGHT, fill=1)
    
    # 顶部装饰条
    c.setFillColor(DARK_BLUE)
    c.rect(0, PAGE_HEIGHT - 0.5 * inch, PAGE_WIDTH, 0.5 * inch, fill=1)
    
    # 标题区域
    title_bg_height = 1.5 * inch
    title_bg_y = PAGE_HEIGHT - 2.5 * inch
    
    c.setFillColor(DARK_BLUE)
    c.rect(0, title_bg_y, PAGE_WIDTH, title_bg_height, fill=1)
    
    # 主标题
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 24)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + 0.5 * inch, "尊享预约")
    
    # 副标题
    c.setFillColor(WHITE)
    c.setFont('SourceHanSans', 14)
    c.drawCentredString(PAGE_WIDTH / 2, title_bg_y + 0.1 * inch, "名额有限，尊享预约")
    
    # 联系信息区域
    contact_y = PAGE_HEIGHT - 5 * inch
    
    c.setFillColor(DARK_BLUE)
    c.setFont('SourceHanSans', 16)
    c.drawCentredString(PAGE_WIDTH / 2, contact_y, "预约咨询")
    
    c.setFillColor(DARK_BLUE)
    c.setFont('SourceHanSans', 14)
    c.drawCentredString(PAGE_WIDTH / 2, contact_y - 0.6 * inch, "请联系您的专属顾问")
    
    # 底部品牌信息
    c.setFillColor(GOLD)
    c.setFont('SourceHanSans', 12)
    c.drawCentredString(PAGE_WIDTH / 2, 1.5 * inch, "大家的家 · 隐湖云舍")
    
    c.setFillColor(DARK_BLUE)
    c.setFont('SourceHanSans', 10)
    c.drawCentredString(PAGE_WIDTH / 2, 1.1 * inch, "杭州 · 西湖畔")
    
    # 底部装饰条
    c.setFillColor(DARK_BLUE)
    c.rect(0, 0, PAGE_WIDTH, 0.3 * inch, fill=1)
    
    c.showPage()
```

- [ ] **Step 2: 测试报名咨询页**

Run: `python3 generate_pdf.py`
Expected: 生成包含报名咨询页的PDF

- [ ] **Step 3: Commit**

```bash
git add generate_pdf.py
git commit -m "feat: implement contact page"
```

---

### Task 11: 完善图片处理和错误处理

**Files:**
- Modify: `generate_pdf.py`

- [ ] **Step 1: 添加图片处理辅助函数**

```python
def draw_image_with_aspect(c, image_path, x, y, width, height):
    """绘制图片，保持宽高比"""
    try:
        img = Image.open(image_path)
        img_width, img_height = img.size
        
        # 计算缩放比例
        ratio = min(width / img_width, height / img_height)
        new_width = img_width * ratio
        new_height = img_height * ratio
        
        # 居中绘制
        draw_x = x + (width - new_width) / 2
        draw_y = y + (height - new_height) / 2
        
        c.drawImage(image_path, draw_x, draw_y, new_width, new_height)
        return True
    except Exception as e:
        print(f"图片绘制失败 {image_path}: {e}")
        return False

def draw_gradient_overlay(c, color, alpha_start, alpha_end, y_start, y_height):
    """绘制渐变遮罩"""
    steps = 20
    step_height = y_height / steps
    
    for i in range(steps):
        alpha = alpha_start + (alpha_end - alpha_start) * (i / steps)
        c.setFillColor(color)
        c.setFillAlpha(alpha)
        c.rect(0, y_start + i * step_height, PAGE_WIDTH, step_height, fill=1)
    
    c.setFillAlpha(1)
```

- [ ] **Step 2: 更新封面页使用新函数**

```python
def create_cover_page(c):
    """创建封面页"""
    # 绘制背景图片
    draw_image_with_aspect(c, "assets/images/cover.jpg", 0, 0, PAGE_WIDTH, PAGE_HEIGHT)
    
    # 绘制渐变遮罩
    draw_gradient_overlay(c, DARK_BLUE, 0.3, 0.9, 0, PAGE_HEIGHT)
    
    # 其余内容保持不变...
```

- [ ] **Step 3: 测试完善后的脚本**

Run: `python3 generate_pdf.py`
Expected: 生成完整PDF，图片处理更稳定

- [ ] **Step 4: Commit**

```bash
git add generate_pdf.py
git commit -m "feat: improve image handling and error recovery"
```

---

### Task 12: 最终测试和优化

**Files:**
- Modify: `generate_pdf.py`

- [ ] **Step 1: 运行完整测试**

Run: `python3 generate_pdf.py`
Expected: 成功生成6页PDF文件

- [ ] **Step 2: 验证PDF内容**

Run: `python3 -c "from pypdf import PdfReader; reader = PdfReader('output/茶禅雅集-老友节高客服务发布会.pdf'); print(f'页数: {len(reader.pages)}')"`
Expected: "页数: 6"

- [ ] **Step 3: 检查文件大小**

Run: `ls -lh output/茶禅雅集-老友节高客服务发布会.pdf`
Expected: 文件大小合理（通常几MB）

- [ ] **Step 4: 最终Commit**

```bash
git add .
git commit -m "feat: complete PDF generation for 茶禅雅集 event"
```

---

## 执行选项

**计划完成，保存到 `docs/superpowers/plans/2026-05-14-茶禅雅集-竖版PDF实施计划.md`**

两种执行方式：

**1. Subagent-Driven（推荐）** - 每个任务分派独立子代理，任务间审查，快速迭代

**2. Inline Execution** - 在当前会话中执行任务，批量执行带检查点

选择哪种方式？